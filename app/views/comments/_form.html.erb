<%= form_for ([@question, @question.answers.comments.new || @question, @question.comments.new  ])  do |f| %>
  <p>
    <%= f.label "Your comment: " %><br>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
