<%= form_for :answer, remote: true, html: { class: "new_answer_comment"} do |f| %>
  <p>
    <%= f.label "Your comment: " %><br>
    <%= f.text_area :content, :class => 'comment_text_area'%>
  </p>
    <input type="hidden" value=<%=@user.id%> name="comment[user_id]"/>
  <p>
    <%= f.submit %>
  </p>
<% end %>
